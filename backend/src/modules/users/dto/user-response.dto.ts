// Re-export the shared interface for use as a response type
// Since we use Prisma `select`, no transformation needed
export type { AuthenticatedUser } from '../../auth/interfaces/authenticated-user.interface';

